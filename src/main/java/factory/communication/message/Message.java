package factory.communication.message;

/**
 * Interface that exists as a way to make sure that the class that is posted through the PostingService is actually a message.
 */
public interface Message {
}
